name: Test

#on: [push]
on:
  push:
    tags: "v*"

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          cargo test --release
